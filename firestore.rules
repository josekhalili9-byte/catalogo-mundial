rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all collections.
    // This is required because the application uses a client-side password
    // instead of Firebase Authentication for the admin panel.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
